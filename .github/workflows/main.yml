name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: e2e

    steps:
      - uses: actions/checkout@v2

      - name: Deploy
        run: docker-compose up -d
